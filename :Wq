#include <stdio.h>
#include <stdlib.h>
int main(int ac, char **argv){
char *Unix = "(Eshell) $ ";
char *lineptr;
  printf("%s", Unix);
  getline(&lineptr, &n, stdin);
  free(lineptr);
  return (0);


}
